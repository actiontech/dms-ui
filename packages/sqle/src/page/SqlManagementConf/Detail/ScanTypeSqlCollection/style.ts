import { styled } from '@mui/material';

export const ScanTypeSqlCollectionStyleWrapper = styled('section')`
  .table-describe-column {
    max-width: 600px;
  }

  .inline-toolbar-table-filter-container {
    padding: 0;
    border-bottom: none;
  }
`;
