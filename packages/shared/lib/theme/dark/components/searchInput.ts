import { SearchInputTheme } from '../../theme.type';
import { darkThemeUI } from '../basic';

const searchInputTheme: SearchInputTheme = {
  searchIconColor: darkThemeUI.uiToken.colorTextQuaternary
};

export default searchInputTheme;
