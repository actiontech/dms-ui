import { BasicResultTheme } from '../../theme.type';
import { darkThemeUI } from '../basic';

const basicResultTheme: BasicResultTheme = {
  title: {
    color: darkThemeUI.uiToken.colorTextBase
  },
  subTitle: {
    color: darkThemeUI.uiToken.colorTextTertiary
  }
};

export default basicResultTheme;
