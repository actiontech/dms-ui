import { CustomDraggerUploadTheme } from '../../theme.type';
import { lightThemeUI } from '../basic';

const customDraggerUploadTheme: CustomDraggerUploadTheme = {
  backgroundColor: lightThemeUI.uiToken.colorFillTertiary,
  color: lightThemeUI.uiToken.colorTextQuaternary,
  border: lightThemeUI.uiToken.colorBorderSecondary
};

export default customDraggerUploadTheme;
