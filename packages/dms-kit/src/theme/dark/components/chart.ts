import { BasicChartTheme } from '../../theme.type';
import { darkThemeBasic } from '../basic';

const basicChartTheme: BasicChartTheme = {
  borderRadius: '8px',
  border: `1px solid ${darkThemeBasic.basic.colorGrayLine}`
};

export default basicChartTheme;
