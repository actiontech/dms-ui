import { CustomDraggerUploadTheme } from '../../theme.type';
import { darkThemeUI } from '../basic';

const customDraggerUploadTheme: CustomDraggerUploadTheme = {
  backgroundColor: darkThemeUI.uiToken.colorFillTertiary,
  color: darkThemeUI.uiToken.colorTextQuaternary,
  border: darkThemeUI.uiToken.colorBorderSecondary
};

export default customDraggerUploadTheme;
